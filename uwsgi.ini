[uwsgi]
module = app:app

http = %(ENV_IP, 0.0.0.0):%(ENV_PORT, 8000)

master = true
processes = %(ENV_PROCESSES, 2)
threads = %(ENV_THREADS, 2)

die-on-term = true

vacuum = true
need-app = true
disable-logging = true
